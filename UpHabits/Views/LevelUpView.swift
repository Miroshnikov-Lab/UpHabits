
import SwiftUI

struct LevelUpView: View {
    var body: some View {
        VStack {
            Text("Level Up")
                .font(.largeTitle)
                .padding()
            Spacer()
        }
    }
}
